export type Project = {
  id: string;
  code: string;
  name: string;
  description: string | null;
};

export type ProjectRow = {
  id: string;
  code: string;
  name: string;
  description: string | null;
};
